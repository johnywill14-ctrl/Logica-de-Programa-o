#include <stdio.h>

int main () {
    
    for (int contador = 0; contador < 5; contador++) {
        printf("%dHello world.\n", contador);
    }

    int contwhile = 0;

    while (contwhile < 5) {
        print("%d: hello world.\n", contwhile)
        contwhile++;
    }

    int contDowhile = 0;

    do {
        printf("%d: Hello world.\n", contDowhile);
        contDowhile++;
    } while (contDowhile < 5);
    return 0;

    while (1) {
        int numero;
    }
}